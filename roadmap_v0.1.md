# 自研 T2Music 执行路线图 v0.1

> 版本：v0.1（2026-09-22）
> 输入：plan.md 技术规划 + 团队现状（企业研发 / PoC 验证 / 暂无数据 / 打基础优先）
> 目标：以最小投入证明技术可行性，建立工程能力，为后续立项评审提供数据支撑。

---

## 团队现状假设

| 维度 | 假设 | 来源 |
|---|---|---|
| 团队 | 企业研发，具备深度学习训练工程能力 | plan.md §1 |
| 当前资产 | 暂无自有授权音乐数据，需从零搭建 | 用户确认 |
| 紧迫需求 | PoC 验证 + 打好基础 | 用户确认 |
| 算力 | 待确认，按"8 卡 × 1 个月"量级估做 PoC | 待用户补充 |

> ⚠️ **待补充信息**：请在推进前确认以下 2 项，否则 PoC 规模与时间线无法敲定。

- [ ] 算力预算与可投入规模（8 卡 / 64 卡 / 数百卡？）
- [ ] 是否允许使用 CC / 非商用数据集做内部验证？商用目标的数据采购时间表？

---

## 0. 总体策略：复用优先、快速验证

```
 ┌────────────────────────────────────────────────────────────┐
 │  阶段 A（第 1-4 周）    复现开源基线 → 建立工程锚点      │
 │  阶段 B（第 5-12 周）   数据管线 v1 + 短曲 PoC           │
 │  阶段 C（第 13-20 周）  全曲扩展 + 架构自研化             │
 │  阶段 D（第 21 周+）    评估对齐 + 迭代 → 立项数据       │
 └────────────────────────────────────────────────────────────┘
```

### 核心原则
1. **不强求从零造轮子**：Apache / MIT 许可的开源组件（ACE-Step v1、YuE、Stable Audio Open 代码）直接复用，加速工程验证。
2. **用最小成本证明链路**：PoC 只验证 text-to-music 器乐，不碰歌词/人声/歌曲。
3. **并行推进**：数据管线与基线复现同步启动，避免串行等待。
4. **保留替换接口**：codec、渲染器、规划器之间定义清晰接口，后期自研模块可直接替换。

---

## 阶段 A：复现基线与工程准备（第 1–4 周）

**阶段目标**：跑通 ≥2 个开源基线的推理 + 训练 pipeline，建立团队对技术栈的体感锚点。

### A.1 复现推理（第 1–2 周）

| 任务 | 基线 | 许可 | 交付物 | 负责 |
|---|---|---|---|---|
| 部署推理 | ACE-Step v1（DCAE + Flow Matching） | Apache 2.0 | 可运行推理 API，示例音频 ≥20 条 | 工程 |
| 部署推理 | YuE2-3B 或 HeartMuLa-3B | 非商用 / 开源 | 可运行推理（关注结构连贯性盲听） | 工程 |
| 对比评估 | 两个基线与 MusicGen + Suno（如可用） | — | 内部盲听评分表（10 人 × 30 prompts） | 评估 |

### A.2 跑通训练 pipeline（第 2–3 周）
- 用公开数据集（MusicCaps / FMA 子集）跑通 ACE-Step v1 的**完整训练流程**（tokenizer + flow matching），确认团队具备多卡训练能力。
- 记录：训练时间、显存、收敛曲线、checkpoint 管理。

### A.3 环境与工具链（第 1–4 周持续）
- 选型并固化：PyTorch + FSDP、W&B 实验追踪、数据集指纹、checkpoint 版本管理。
- 交付：团队共用训练模板 repo（README + 启动脚本）。

### 阶段 A 验收标准
- [ ] 至少 2 个基线的推理可复现（输出音频可听）
- [ ] 至少 1 个训练 pipeline 可跑通（能出 checkpoint）
- [ ] 内部盲听锚点文档完成（基线质量基线值）
- [ ] 共用工程模板具备可复用性

---

## 阶段 B：数据管线 v1 + 短曲 PoC（第 5–12 周）

**阶段目标**：建成自动标注流水线 + 产出一个可用的 30–60s 器乐 text-to-music 模型的 PoC。

### B.1 数据管线（第 5–10 周）
> 这是企业研发最容易低估的工作量，建议投入 ≥1 人专职。

1. **数据源**：
   - 内部：与合作方/版权方谈判（如需商用）
   - 公开：MusicCaps（5.5k × 1s）、FMA（10 万 +）、Jamendo（仅内部验证）、公共领域曲目
   - **合规红线**：MTG-Jamendo 不进入商用训练集，仅用于学术研究验证；后期商用需独立采购授权（参考 plan.md §9 风险表）。
2. **自动标注**（参考 plan.md §4.2）：
   - 音频清洗（响度归一、去重、掐头去尾）
   - 标签生成（CLAP + Qwen-Audio/MuMu-LLaMA caption）
   - 结构切分（段落检测）
3. **质量分层**：L1（人工核验高置信）/ L2（自动高置信）/ L3（海量弱标注）
4. **目标产出**：清洗对齐样本 ≥10 万条（30–60s 切片），其中 L1 高置信 ≥5000 条。

### B.2 短曲 PoC 训练（第 7–12 周，与 B.1 并行）
- 数据：MusicCap 扩产 + 公开数据集子集（约 1k–5k 小时）
- 模型：基于 ACE-Step v1 的 DCAE + Flow Matching，复用其 vocoder，只替换或为改进条件注入做定制。
- 规模：渲染器 1B 起步（参考 ACE-Step 1 的 3.5B，但 PoC 先用 1B 降低算力门槛）。
- 算力需求（估）：8 卡 A100 × 4–6 周 ≈ 2000–3000 卡时。

### 阶段 B 验收标准
- [ ] 数据管线 v1 可自动跑通，每日产出 ≥1000 条标注样本
- [ ] 100 条人工核验标签准确率 ≥85%
- [ ] PoC 模型生成 30–60s 音频，FAD/CLAP 与 MusicGen + Stable Audio Open 持平
- [ ] 内部盲听 MOS ≥ 3.5 / 5

---

## 阶段 C：全曲扩展与架构自研化（第 13–20 周）

**阶段目标**：从"短曲 generation"扩展到"全曲 structure"，并开始替换开源组件为自研模块。

### C.1 全曲渲染扩展
- 扩展上下文长度到支持 ≥3 分钟结构
- 引入段落级条件（intro/verse/chorus 标签）

### C.2 规划器引入（可选，根据 B 阶段结果决定）
- 若 B 阶段结构连贯性不足 → 引入小 LM 做蓝图规划（YuE2 / ACE-Step 1.5 形态）
- 若 B 阶段质量已够用 → 优先做 prompt 工程与条件注入优化，推迟规划器

### C.3 自研模块优先级
| 模块 | 复用方案 | 自研替代时机 |
|---|---|---|
| Codec / DCAE | ACE-Step DCAE | 当重建 FAD 成为瓶颈时 |
| 渲染器 | ACE-Step / Diffusion | 当结构连贯性或推理速度不达标时 |
| 规划器 | / | 产品化阶段，非 PoC 重点 |

### 阶段 C 验收标准
- [ ] ≥2 分钟音频生成，结构连贯性盲听通过率 ≥60%
- [ ] 自研模块替换路径明确（interface 已定义）
- [ ] 至少 1 项自研改进（数据、训练策略、工程优化）的可量化对比结果

---

## 阶段 D：评估对齐与立项输出（第 21 周起）

**阶段目标**：形成可用于内部立项评审的完整数据包。

### D.1 评估体系落地
- 客观：FAD / CLAP score / CLaMP3 / 结构一致性（counterfactual 测试）
- 主观：固定 prompt 集 192 条盲听 MOS（参考 WildSongBench）
- 输出：与 YuE2 / HeartMuLa / LeVo 2 的**可对比评估报告**

### D.2 立项材料
- 技术可行性结论（已验证 / 不可行 / 需调整路线）
- 已有能力清单（工程、数据、模型 checkpoint）
- 后续投入预估（M4+ 的算力、人力、时间）
- 风险清单与应对（法务、数据许可、算力、人员）

---

## 关键风险与应对（针对企业研发 / PoC 现状）

| 风险 | 等级 | 触发条件 | 应对预案 |
|---|---|---|---|
| **算力预算未按期到位** | 高 | 阶段 A 启动后无法获得 GPU | 阶段 A 只做推理复现（不需要算力），训练 pipeline 推到阶段 B |
| **数据许可法律风险** | 高 | 法务审核不通过商用数据 | PoC 阶段仅用 CC / 公共领域数据，商用训练单独走采购流程 |
| **团队技术栈不匹配** | 中 | 首次接触 audio / music domain | 阶段 A 用 ACE-Step 官方镜像跑通，避免过早深入底层 |
| **领域迭代快导致"刚做出来就落后"** | 中 | 新开源模型性能跳升 | 不追 SOTA，只追"可解释、可改、可工程化"的基线 |
| **跨团队协同低效** | 中 | 数据 / 算力 / 评估分散在不同组 | 设 1 人专职 project manager，阶段评审固定节奏双周一次 |

---

## 里程碑评审节奏

| 里程碑 | 时间 | 评审内容 | Go / No-Go 标准 |
|---|---|---|---|
| M-A | 第 4 周 | 基线复现 + 环境是否 ready | ≥1 个基线跑通；工程模板可用 |
| M-B1 | 第 8 周 | 数据管线 v1 demo | 自动标注跑通 + 抽样核验 ≥85% |
| M-B2 | 第 12 周 | 短曲 PoC 首版 | FAD/CLAP 对标开源；盲听 MOS ≥ 3.5 |
| M-C | 第 20 周 | 全曲扩展 + 自研启动 | 结构连贯、立项数据完整 |
| M-D | 第 24 周 | 能否进入产品化立项 | 评估报告 + 投入产出分析 |

---

## 立即行动（本周内）

1. **确认算力预算**：这是路线图的硬约束。尽快获得 8 卡 / 64 卡云租用报价。
2. **指定项目负责人**：至少需要 1 个 PM（或技术负责人兼）专职推进。
3. **法务前置介入**：确认 CC 非商用数据集用于内部研究的合规边界，明确商用数据采购时间表。
4. **阶段 A 启动**：拉人开始复现 ACE-Step v1 和 YuE2 推理，不依赖算力。

---

## 与 plan.md 的对应关系

| plan.md 章节 | 路线图对应 | 备注 |
|---|---|---|
| §3 技术选型 | 阶段 A–D 主线 | 混合式路线，PoC 先从连续潜空间 + Flow Matching 切入 |
| §4 数据方案 | B.1 数据管线 | 强调合规红线、自动标注、质量分层 |
| §5 训练管线 | B.2 / C | 对齐 P0–P4，但 PoC 只做到 P1 短曲验证 |
| §6 算力估算 | 待用户补充 | 先按 8 卡估，贵团队预算确定后回填 |
| §7 里程碑 | M-A ~ M-D | 从 plan.md 的 M0–M6 拆分重组，以 4 周评审为节奏 |
| §9 风险表 | 风险与应对 | 补充了"团队技术栈"和"跨团队协同"等企业场景风险 |

---

> 本路线图假设在**第 12 周末（M-B2）**做出首次 Go/No-Go 判断。如果 PoC 质量不达标，按 plan.md §3.2 切换路线；如果达标，再推动立项进入 M4+（全曲 + 商用路线）。
